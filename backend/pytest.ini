[pytest]
markers =
    integration: real network + Gemini calls (slow, run explicitly)

# Integration tests time out after 3 minutes each
timeout = 180

# Don't run integration tests by default
addopts = -m "not integration"
